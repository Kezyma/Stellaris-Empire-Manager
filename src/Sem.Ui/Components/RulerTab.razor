@*
    The empire's first ruler: who they are, how they look, and what they are good at.

    Three components rather than three blocks of markup, because the lite card opens them one at a
    time — pressing the ruler's face asks about their face and nothing else.
*@

<div class="sem-subsections">
    <section>
        <h4>@Session.Localizer.Label("LEADER_NAME", "Leader Name")</h4>

        <RulerNaming />
    </section>

    <section>
        @*
            Titled in plain words rather than the game's, because the game has none for this. Its
            PORTRAIT reads "Species & Portrait" and belongs to the species editor; the ruler's
            appearance is a screen of its own with no heading, only the labels on its controls.
        *@
        <h4>Appearance</h4>

        <RulerAppearance />
    </section>

    <section>
        <RulerTraits />
    </section>
</div>

@code {
    [CascadingParameter]
    public required DesignSession Session { get; set; }
}
